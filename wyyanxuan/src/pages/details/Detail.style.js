import styled from "styled-components"

export const Item = styled.div `
    .detail {
        height: calc(100vh - 0.65rem);
        width: 100%;
        .detail-padding {
            padding-top: 2px;
        }
    }
`